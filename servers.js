const express = require("express")

const server = express()

server.all("/", (req, res) => {
  res.send('The Bot is Ready Now ┃  Please Follow me on Discord: apgr.#0840')
})

function server.js() {
  server.listen(3000, () => {
    console.log("Creator: AnonymousPlaysGR")
  })
}

module.exports = keepAlive
